# SIMS Doctor v1.0.0 Sprint8.3

Production Queue Adapter and Scheduler CLI release.

Doctor can now preserve queued work in SQLite, survive process restarts, and run from a
scheduler-compatible command line with operational logs and explicit exit codes.
